console.log("1" == 1); //true
console.log("1" === 1); //false

//"1" == 1 is true because JavaScript performs type coercion and converts the string to a number.
//"1" === 1 is false because it's a strict comparison, and the data types are different